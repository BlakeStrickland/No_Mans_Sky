<%= form_for(@multitool) do |f| %>
  <% if @multitool.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@multitool.errors.count, "error") %> prohibited this multitool from being saved:</h2>

      <ul>
      <% @multitool.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
